import { RegistroList } from "../components/RegistroList";

/**
 * Página que muestra la lista de registros.
 * Utiliza el componente RegistroList para representar visualmente la lista.
 * @component
 * @returns {JSX.Element} - Elemento JSX que representa la página de registros.
 */
export function RegistroPage() {
  return <RegistroList />;
}
